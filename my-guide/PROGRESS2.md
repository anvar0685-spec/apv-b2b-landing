# PROGRESS2 — доведение аудита до 100% (2026-05-07)

Трекер закрытия недостатков по ролям **02–05**, технике и прод-готовности. Статусы: `[ ]` → `[x]`.

## P0 — SEO и URL (роль 04)

- [x] `absUrl` / canonical / sitemap / OG image — префикс `/ru` согласован с `localePrefix: "always"`.
- [x] JSON-LD (главная, услуги, блог, контакты, лиды): абсолютные URL совпадают с публичными путями.
- [x] Обновлён фрагмент `SEO-AUDIT-FULL-2026-04-23.md` (§ локаль).

## P0 — Коммерция и инфра (роль 03 / прод)

- [x] Код воронки и API лидов без регрессий (проверка `npm run build`).
- [ ] **Прод-окружение (вне кода):** боевой `NEXT_PUBLIC_SITE_URL`, HTTPS, БД + seed тенанта, смоук POST лида, Метрика — остаётся на стороне хостинга/заказчика; задеплоенный код готов принять env.

## P1 — Копирайт и витрина (роли 05 + 02)

- [x] Главная `ru.json`: убраны «контур поставки», выровнены формулировки под операционный русский; дисклеймеры без лишнего «поставка» в UX.
- [x] Хаб услуг уже в человеческом тоне — без изменений.
- [ ] Юридическая вычитка документов — только человек-юрист (`legal-site-documents.ts`).

## P1 — UX / перфоманс (роль 02)

- [x] Страница `/zayavka`: динамический импорт формы (`next/dynamic`, `ssr: true`) — отдельный чанк; тяжёлая логика формы не в initial shared bundle.

## P2 — Ассистент и консистентность

- [x] `/ru/keysy` в базе знаний и linkify; перегенерирован `site-assistant-knowledge.generated.ts`.

## Закрытие

- [x] `npm run build` зелёный (локально и на VPS).
- [x] Git commit `f423ae0` + push `main`.
- [x] Деплой на VPS: `git pull`, `npm ci`, `build:vps`, `pm2 reload apv-b2b-landing`; `curl -sI http://127.0.0.1:3000/` → **307** на `/ru` (ожидаемо).

**Итог 100% по коду:** выполнено. **Итог 100% по бизнес/прод:** после выставления env и смоук-тестов на сервере (чеклист в `PROGRESS.md`).
