# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# локальный резерв старых сток-фото на главной (не идёт в прод)
public/home/industrial-band/_old-stock/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# SSH VPS (Timeweb root password — только локально, никогда в репозиторий)
deploy/.vps-ssh.local.env
deploy/.vps-smtp-append.env

# Яндекс OAuth (Client secret — только локально; для скриптов API Вебмастера)
deploy/.yandex-oauth.local.env
# одноразовый код подтверждения OAuth (вставить → npm run webmaster:exchange-file)
deploy/.yandex-oauth-code.local
deploy/.metrica-oauth.local.env

# Google Search Console API (Client secret — только локально)
deploy/.google-oauth.local.env
deploy/.google-oauth-code.local
deploy/gsc-routine-latest.json
deploy/.seo-automation-state.json
deploy/.seo-automation.env.local
deploy/seo-daily-latest.json
deploy/seo-daily-launchd.log
my-guide/SEO-CHECK-latest.md
my-guide/SEO-DAILY-latest.md

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

scripts/__pycache__/
